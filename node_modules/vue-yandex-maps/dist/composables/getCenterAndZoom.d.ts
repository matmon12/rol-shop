import type { LngLat, LngLatBounds, YMap } from '@yandex/ymaps3-types';
export declare function useYMapsLocationFromBounds({ bounds, map, }: {
    bounds: LngLatBounds;
    map: YMap;
}): Promise<{
    zoom: number;
    center: LngLat;
}>;
